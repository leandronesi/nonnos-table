# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Env
.env

# Data (PGN/analisi grezzi possono pesare; metrics.json lo lasciamo tracciabile)
data/raw/
data/analysis/
data/index.json
data/analyze.log
# data/metrics.json   → lo committiamo: serve per il primo deploy

# Le analisi singole per il drill-down su gh-pages le rigenera la GH Action.
# 8 MB di JSON: meglio non averli nel repo principale.
frontend/public/analysis/

# Stockfish + lc0 binari locali (root engine/, NON frontend/src/engine o frontend/public/engine)
/engine/

# COACH PRIVATO: i prompt + wiki del coach LLM stanno in repo privato separato.
# In CI vengono clonati con un PAT secret (vedi workflow refresh-and-deploy.yml).
# Per dev locale: il coach_brain vive qui ma è gitignored.
backend/coach_brain/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/*.tsbuildinfo
*.tsbuildinfo

# Editor
.vscode/
.idea/
.DS_Store
Thumbs.db
